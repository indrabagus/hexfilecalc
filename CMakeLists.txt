CMAKE_MINIMUM_REQUIRED(VERSION 2.4)
PROJECT("Hex File Calculator")
SET(CMAKE_VERBOSE_MAKEFILE ON)

INCLUDE_DIRECTORIES(SYSTEM 
					${PROJECT_SOURCE_DIR})

ADD_EXECUTABLE(hexfilecalc hexfilecalc.c )